streamlit==1.45.0       # Web framework (UI, routing, widgets)
pandas==2.2.3           # Data manipulation and analysis
plotly==6.0.1           # Interactive charts and visualizations
pdfplumber==0.11.6      # PDF text extraction (receipts, statements)
PyPDF2==3.0.1           # PDF reading utilities
pytesseract==0.3.13     # OCR engine bridge (optional — requires Tesseract)
Pillow==11.1.0          # Image processing (receipt photos, logo handling, tray icon)
openpyxl==3.1.5         # Excel file reading (.xlsx)
yfinance==0.2.54        # Stock market data (Yahoo Finance API)
requests==2.32.3        # HTTP requests (crypto prices, update checks)
rapidfuzz==3.12.2       # Fuzzy string matching (subscriptions, search)
fpdf2==2.8.3            # PDF generation (invoices, reports, guides)
xlsxwriter==3.2.2       # Excel file writing (.xlsx exports)
kaleido==0.2.1          # Plotly chart export to static images
bcrypt==4.2.1           # Password hashing (authentication)
pystray==0.19.5         # System tray icon (Windows launcher)
